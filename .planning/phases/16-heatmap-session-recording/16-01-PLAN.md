---
phase: 16-heatmap-session-recording
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/diagram/types.ts
  - src/diagram/annotations.ts
  - src/diagram/service.ts
  - src/session/session-types.ts
  - src/session/session-store.ts
  - src/server/websocket.ts
  - src/server/session-routes.ts
  - src/server/routes.ts
  - src/server/server.ts
autonomous: true

must_haves:
  truths:
    - "@risk NodeId high|medium|low 'reason' annotations are parsed from .mmd files"
    - "DiagramService can get/set/remove risk annotations while preserving all other annotation types"
    - "SessionStore can start, record, and end sessions as JSONL files in .smartb/sessions/"
    - "REST endpoints serve session lists, session events, and heatmap aggregation data"
    - "WebSocket broadcasts session:event and heatmap:update messages"
  artifacts:
    - path: "src/session/session-types.ts"
      provides: "SessionEvent, SessionMeta, SessionSummary, SessionEventType types"
      contains: "SessionEvent"
    - path: "src/session/session-store.ts"
      provides: "SessionStore class with JSONL persistence"
      exports: ["SessionStore"]
    - path: "src/server/session-routes.ts"
      provides: "Session and heatmap REST endpoints"
      contains: "registerSessionRoutes"
    - path: "src/diagram/annotations.ts"
      provides: "parseRisks() function and RISK_REGEX"
      contains: "RISK_REGEX"
  key_links:
    - from: "src/server/session-routes.ts"
      to: "src/session/session-store.ts"
      via: "SessionStore methods"
      pattern: "sessionStore\\.(startSession|readSession|getHeatmapData)"
    - from: "src/diagram/service.ts"
      to: "src/diagram/annotations.ts"
      via: "parseRisks and injectAnnotations"
      pattern: "parseRisks|injectAnnotations.*risks"
    - from: "src/server/server.ts"
      to: "src/session/session-store.ts"
      via: "SessionStore instantiation"
      pattern: "new SessionStore"
---

<objective>
Backend infrastructure for @risk annotations, JSONL session store, REST endpoints, and WebSocket message types.

Purpose: Provide the server-side foundation that MCP tools (Plan 02) and frontend modules (Plans 03-04) build upon.
Output: Types, annotation parsing, SessionStore class, 3 REST endpoints, 2 new WebSocket message types.
</objective>

<execution_context>
@/Users/simoni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simoni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-heatmap-session-recording/16-RESEARCH.md
@.planning/phases/15-ai-breakpoints-ghost-paths/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: @risk annotation types, parsing, and DiagramService CRUD</name>
  <files>
    src/diagram/types.ts
    src/diagram/annotations.ts
    src/diagram/service.ts
  </files>
  <action>
**types.ts** (~86 lines after):
- Add `RiskLevel = 'high' | 'medium' | 'low'`
- Add `RiskAnnotation = { nodeId: string; level: RiskLevel; reason: string }`

**annotations.ts** (~270 lines after):
- Add `RISK_REGEX = /^%%\s*@risk\s+(\S+)\s+(high|medium|low)\s+"([^"]*)"$/`
- Add `parseRisks(content: string): Map<string, RiskAnnotation>` following exact pattern of parseFlags/parseStatuses/parseBreakpoints (iterate lines inside annotation block, match regex, build Map)
- Extend `injectAnnotations()` signature: add optional `risks?: Map<string, RiskAnnotation>` parameter after `breakpoints`. Emit risk lines as `%% @risk {nodeId} {level} "{reason}"` after breakpoint lines, inside annotation block. Escape double quotes in reason with `''` (same pattern as flag message escaping).
- Export parseRisks and update all exports

**service.ts** (~330 lines after):
- Add `getRisks(file: string): Promise<Map<string, RiskAnnotation>>` -- reads raw content, calls parseRisks
- Add `setRisk(file: string, nodeId: string, level: RiskLevel, reason: string): Promise<void>` -- reads content, parses all 4 annotation types (flags, statuses, breakpoints, risks), updates risk Map, calls writeDiagram with injectAnnotations passing all 4 types
- Add `removeRisk(file: string, nodeId: string): Promise<void>` -- same read-all-4-types pattern, deletes from risk Map, writes back
- CRITICAL: Update existing setBreakpoint/removeBreakpoint/setFlag/removeFlag/setStatus/removeStatus methods to also read and pass through risks (four-annotation preservation, extending the three-annotation pattern from Phase 15). Each of these methods must now call parseRisks(raw) and pass risks to injectAnnotations.
  </action>
  <verify>
Run `npm run typecheck` -- no errors. Run `npm test` -- all 238 existing tests pass (no regressions). Manually verify: `grep -n "RISK_REGEX\|parseRisks\|getRisks\|setRisk\|removeRisk" src/diagram/annotations.ts src/diagram/service.ts` shows all functions present.
  </verify>
  <done>
@risk annotations are parsed from .mmd files. DiagramService provides getRisks/setRisk/removeRisk CRUD. All existing annotation write operations preserve all 4 annotation types (flags, statuses, breakpoints, risks).
  </done>
</task>

<task type="auto">
  <name>Task 2: SessionStore, session types, REST endpoints, and WebSocket message types</name>
  <files>
    src/session/session-types.ts
    src/session/session-store.ts
    src/server/websocket.ts
    src/server/session-routes.ts
    src/server/routes.ts
    src/server/server.ts
  </files>
  <action>
**src/session/session-types.ts** (NEW, ~50 lines):
- Define `SessionEventType` union: `'session:start' | 'session:end' | 'node:visited' | 'edge:traversed' | 'status:changed' | 'flag:added' | 'flag:removed' | 'risk:set' | 'node:added' | 'node:removed' | 'edge:added' | 'edge:removed'`
- Define `SessionEvent = { ts: number; type: SessionEventType; payload: Record<string, unknown> }`
- Define `SessionMeta = { id: string; diagramFile: string; startedAt: number }`
- Define `SessionSummary = { sessionId: string; diagramFile: string; duration: number; totalEvents: number; nodesVisited: number; uniqueNodesVisited: number; edgesTraversed: number }`
- Export all types

**src/session/session-store.ts** (NEW, ~180 lines):
- Import `appendFile`, `readFile`, `readdir`, `mkdir` from `node:fs/promises`, `join` from `node:path`, `randomUUID` from `node:crypto`
- Class `SessionStore` with constructor(projectRoot: string) setting `this.sessionsDir = join(projectRoot, '.smartb', 'sessions')`
- Private `activeSessions = new Map<string, SessionMeta>()`
- `ensureDir()`: `await mkdir(this.sessionsDir, { recursive: true })`
- `startSession(diagramFile: string): Promise<string>`: ensureDir, create UUID, store meta, append session:start event, return id
- `recordStep(sessionId: string, event: SessionEvent): Promise<void>`: append event as JSONL line
- `endSession(sessionId: string): Promise<SessionSummary>`: read events, append session:end event, delete from activeSessions, compute and return summary
- `readSession(sessionId: string): Promise<SessionEvent[]>`: readFile, split by newline, JSON.parse each line, filter empty
- `listSessions(diagramFile: string): Promise<string[]>`: readdir sessions dir, for each .jsonl file read first line to check if diagramFile matches, return matching session IDs
- `getHeatmapData(diagramFile: string): Promise<Record<string, number>>`: list sessions for file, read each, count node:visited events by nodeId, return counts
- `getActiveSession(sessionId: string): SessionMeta | undefined`: return from activeSessions Map
- Private `filePath(sessionId)`: join sessionsDir with `${sessionId}.jsonl`
- Private `computeSummary(events, sessionId, diagramFile)`: count node:visited, unique nodes, edge:traversed, duration
- Use the per-session write lock pattern: add `private writeLocks = new Map<string, Promise<void>>()` and a `withWriteLock(sessionId, fn)` method (same pattern as DiagramService) to prevent concurrent JSONL corruption

**src/server/websocket.ts** (~130 lines after):
- Add to WsMessage union type: `| { type: 'session:event'; sessionId: string; event: Record<string, unknown> }` and `| { type: 'heatmap:update'; file: string; data: Record<string, number> }`

**src/server/session-routes.ts** (NEW, ~80 lines):
- Export function `registerSessionRoutes(routes: Route[], sessionStore: SessionStore)` that adds 3 routes:
  1. `GET /api/sessions/:file` -- list sessions for a diagram file. Call `sessionStore.listSessions(file)`, return `{ sessions: [...] }`
  2. `GET /api/session/:id` -- get full session events. Call `sessionStore.readSession(id)`, return `{ events: [...] }`
  3. `GET /api/heatmap/:file` -- get heatmap aggregation. Call `sessionStore.getHeatmapData(file)`, return the counts object
- Use the same `sendJson` helper pattern from routes.ts. Import the Route type from routes.ts (or define locally).
- Handle errors with try/catch, return 404 for missing sessions, 500 for server errors

**src/server/routes.ts** (~475 lines after):
- Import `registerSessionRoutes` from `./session-routes.js`
- After existing route registrations, call `registerSessionRoutes(routes, sessionStore)` where sessionStore is passed into registerRoutes
- Update `registerRoutes` function signature to accept `sessionStore: SessionStore` parameter

**src/server/server.ts** (~330 lines after):
- Import `SessionStore` from `../session/session-store.js`
- In `createHttpServer`, create `const sessionStore = new SessionStore(projectRoot)` (use first project's root, or a server-level .smartb dir)
- Pass sessionStore to `registerRoutes()`
- Expose sessionStore on the `ServerInstance` interface and return value (same pattern as ghostStore and breakpointContinueSignals)
  </action>
  <verify>
Run `npm run typecheck` -- no errors. Run `npm test` -- all tests pass. Verify: `grep -rn "registerSessionRoutes\|SessionStore\|session:event\|heatmap:update" src/` shows all artifacts present. Verify routes.ts is under 500 lines: `wc -l src/server/routes.ts`.
  </verify>
  <done>
SessionStore persists events as JSONL in .smartb/sessions/. Three REST endpoints serve session data and heatmap aggregation. Two new WebSocket message types are defined. SessionStore is instantiated in the server and exposed on ServerInstance for MCP tool access.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with zero errors
2. `npm test` passes all 238+ tests with zero regressions
3. `wc -l src/diagram/annotations.ts` < 300, `wc -l src/diagram/service.ts` < 350, `wc -l src/server/routes.ts` < 500
4. `grep "RISK_REGEX\|parseRisks\|getRisks\|setRisk" src/diagram/annotations.ts src/diagram/service.ts` confirms all functions exist
5. `grep "SessionStore\|registerSessionRoutes" src/server/server.ts src/server/routes.ts src/server/session-routes.ts` confirms wiring
6. `grep "session:event\|heatmap:update" src/server/websocket.ts` confirms new WS message types
</verification>

<success_criteria>
- @risk annotations parse correctly from .mmd content and survive round-trip injection
- All 4 annotation types (flags, statuses, breakpoints, risks) are preserved by every write operation
- SessionStore creates .smartb/sessions/ directory and writes JSONL files
- REST endpoints return session lists, session events, and heatmap data
- All files under 500-line limit
</success_criteria>

<output>
After completion, create `.planning/phases/16-heatmap-session-recording/16-01-SUMMARY.md`
</output>
